alternate: 拥抱过去，创造未来

menu:
  home: / || home
  posts:
    default: / || feather
    archives: /archives/ || list-alt
    categories: /categories/ || th
    tags: /tags/ || tags

social:
  github: https://github.com/Rebirth0Rookie || github || "#191717"

sidebar:
  # Use a bundled asset until a personal avatar is added to source/_data/assets/.
  avatar: logo.svg

reward:
  # Enable this only after adding payment QR code images to source/_data/assets/.
  enable: false

modules:
  # No playlist is configured, so keep the player UI disabled.
  player: false

font:
  title:
    external: true
    family: Noto Serif
    size: 2.5
  headings:
    external: true
    family: Noto Serif SC
